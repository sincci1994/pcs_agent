# PCS Agent UI/UX 정리 및 클로드 디자인 프롬프트

> `pcs_agent_planning.md` 기획서를 기반으로, 챗 UI + 시각화 캔버스 형태의 화면 초안을
> 클로드 디자인(claude.ai)에서 HTML로 생성하기 위한 요구사항 정리 및 입력 프롬프트.
>
> v2: PCS기술팀 도메인 지식 반영 — 그룹군 계층 구조, 교체주기 기반 임계값,
> 레시피별 사용량, 설비 상태 파라미터(상압 대비 압력 / 보조 히팅센서 온도).
>
> v3: 판단 기준 정의서([`pcs_agent_judgment_criteria.md`](./pcs_agent_judgment_criteria.md))와
> 계통도 문서([`pcs_system_diagram.md`](./pcs_system_diagram.md)) 반영 —
> ① 계통도 위젯을 8종 노드/6종 배관 그래프로 상세화(점검 구간 하이라이트, BYP 경로, Pair),
> ② 상태 분류를 하드코딩이 아닌 판단 케이스(J-매트릭스) 규칙 함수로 계산,
> ③ Pair/바이패스 mock 스토리와 챗 시나리오 추가.

---

## 1. UI/UX 방향 정리

### 1.1 왜 분할형 하이브리드인가

엔지니어의 실제 워크플로우는 다음 세 가지가 동시에 일어난다.

1. 담당 그룹군 전체 상황(사용률, Pressure, 작업 이력)을 **동일한 기준으로 시각적으로 훑어본다.**
2. 눈에 걸리는 설비에 대해 **Agent에게 자연어로 질문한다.**
3. Agent의 답변을 보면서 **원하는 시각화를 추가로 요구한다.**

챗만 있는 UI는 1번(전체 상황 파악)에 약하고, 대시보드만 있는 UI는 2~3번(탐색적 질의)에 약하다.
따라서 **좌측 챗 패널 + 우측 시각화 캔버스**의 분할형 하이브리드를 기본 레이아웃으로 한다.

```text
┌────────────┬──────────────────────────────────────┐
│  💬 Chat   │  그룹군 컨텍스트 바                      │
│  (~30%)    │  (사업부/사이트/라인/공정/PRC그룹/기간)   │
│            ├──────────────────────────────────────┤
│  대화 이력  │  📊 [그룹 개요 존] KPI·산점도·후보 테이블 │
│  추천질문 칩├──────────────────────────────────────┤
│  [입력창]  │  📊 [선택 설비 상세 존] 시계열·계통도·    │
│            │     게이지·레시피·상태 파라미터           │
└────────────┴──────────────────────────────────────┘
```

### 1.2 그룹군 기준으로 보기 (전역 컨텍스트 바)

PCS기술팀은 반도체 설비를 **그룹군 계층 구조**로 바라본다. 캔버스 상단의 전역 컨텍스트 바는
이 계층을 따라가는 **cascading 셀렉터**로 구성한다.

```text
사업부(메모리/파운드리) → 사이트(평택/화성/기흥) → 라인 → 공정 → PRC그룹
```

- 사이트-라인 종속 관계: 평택 → P1, P2 / 화성·기흥 → 15L, 16L, 17L
- 공정: DIFF, CVD, METAL, ETCH
- PRC그룹: ALD_OX, TEOS 등 (공정에 종속)
- 여기에 기간 필터(최근 7일/30일)와 **데이터 기준 시점** 상시 표시(예: `기준시점: 2026-07-04 14:00`)

**기본 뷰는 라인 단위다.** 라인이 달라도 PRC그룹이 동일하면 기술적으로는 함께 볼 수 있지만,
라인마다 책임지는 엔지니어가 다르기 때문에 기본은 자신의 라인만 본다.
타 라인 비교는 "타 라인 동일 PRC그룹 비교" 토글 같은 **보조 기능**으로만 제공한다.

캔버스의 **모든 위젯이 이 그룹군 컨텍스트를 공유**한다. 챗으로 조건을 바꾸면 컨텍스트 바도 함께 바뀐다.
Agent 답변에도 항상 그룹군 조회 조건과 기준 시점을 명시한다(기획서 5.2 답변 수준).

### 1.3 핵심 개념: 교체주기 기반 추정 임계값

현재 정식 관리 기준값은 없지만(기획서 4.2), 각 PCS 설비는 **사용량에 따른 교체주기** 정보를 갖고 있다.
이 교체주기로부터 **대략적인 가스 사용량 임계값을 추정**할 수 있고,
"최근 교체/작업 이후 누적 사용량이 추정 임계값 대비 몇 %인가"로 현재 상황을 본다.

- 사용량 축은 원시 누적량이 아니라 **추정 임계값 대비 사용률(%)** 로 정규화한다.
  → 설비·레시피가 달라도 동일 잣대로 비교 가능하고, 기준선을 "80%" 같은 직관적 위치에 둘 수 있다.
- 누적 사용량은 교체/클리닝 작업 시점에 **리셋**되는 개념이다(작업 이후 누적).
- 임계값은 어디까지나 **추정치**이므로 UI와 Agent 답변 모두 "추정 임계값" 라벨을 명시한다.

### 1.4 판단 로직을 프로토타입에 내장한다 (v3 핵심)

상태(정상/관찰 필요/점검 후보/원인 확인 필요/우선 점검 후보)를 mock 데이터에
**하드코딩하지 않는다.** mock에는 원시 값(사용률, 압력, 히팅센서, 작업 이력, Pair/BYP 상태)만 넣고,
상태는 판단 기준 정의서의 **판단 케이스 매트릭스(J-01~J-09)와 가점제 스코어링을 구현한
규칙 함수**로 계산한다.

이렇게 하는 이유:

- 기준선(70%/85% 등)을 드래그로 옮기면 상태 분류가 **실제로 재계산**된다
  → 워크숍에서 "기준을 어디에 둘 것인가"(P-01/P-02) 논의가 화면에서 즉시 검증된다.
- 프로토타입의 규칙 함수가 곧 **Phase 3 Rule Engine의 초기 Rule 후보 구현체**가 된다.
- Agent 답변의 "판단 근거"가 규칙 결과에서 나오므로, 화면·테이블·챗 답변의
  상태가 항상 일치한다.

모든 경계 수치는 판단 기준 정의서 §5의 **가정값(P-01~P-08)** 이며 UI에 "임시 기준" 라벨을 붙인다.

### 1.5 핵심 위젯 8종 — 2단 구성 (기획서 3.2 업무 단계 매핑)

실제 업무 흐름(그룹군 전체 훑기 → 특정 설비 파고들기)에 맞춰 캔버스를 2단으로 나눈다.

**[상단: 그룹 개요 존]** — 선택된 그룹군(라인/공정/PRC그룹) 전체를 본다.

| # | 위젯 | 대응 업무 단계 | 역할 (판단기준 문서 연계) |
|---|---|---|---|
| 1 | 상태 요약 KPI 타일 | 7. 점검 후보 도출 | 규칙 함수로 계산된 상태별 개수 (J-매트릭스 결과 집계) |
| 2 | 사용률(%) vs Pressure 산점도 | 3~4. 사용량·압력 확인 | X축=추정 임계값 대비 사용률(%) = D1×D2 평면. 4분면이 J-01/J-02/J-03·04 후보 분포와 대응 |
| 3 | 점검 후보 테이블 | 6~7. 원인 후보·우선순위 | 스코어 정렬 + 판단 케이스 ID, 근거 신호, 점검 대상 구간, 원인 후보, 추가 확인 항목 |

**[하단: 선택 설비 상세 존]** — 산점도/테이블에서 설비를 선택하면 갱신된다.

| # | 위젯 | 대응 업무 단계 | 역할 (판단기준 문서 연계) |
|---|---|---|---|
| 4 | Pressure 시계열 차트 | 4~5. 압력·작업 이력 | 작업 마커 + 상압 기준선 → 재상승(P-05)·추세(P-04) 패턴 확인 |
| 5 | 배기 경로 계통도 (8종 노드/6종 배관) | 2. 계통 구조 확인 | 정상/바이패스 경로 표시, **점검 대상 구간(pipe) 하이라이트**(B1), Pair 상대 설비 표시(B4) |
| 6 | 교체주기 대비 사용량 게이지 | 3. 사용량 확인 | D1 게이지. 리셋 기준 작업 표시. BYP 미보정 시 주석(B3) |
| 7 | 레시피별 사용량 테이블 | 3. 사용량 확인 | 레시피별 peak/1주기당 사용량 — peak 상위 레시피(P-07)가 D5 가중 요인 |
| 8 | 설비 상태 파라미터 패널 | 6. 원인 후보 판단 | 배기 압력 vs 상압(P-03 자리), 히팅센서 온도를 **구간(FORELINE/PS/SD1 등) 매핑**과 함께 표시(D3×B1) |

산점도의 4분면은 기준값이 없는 현 단계에서 **상대 비교 기반 관리 기준점을 논의하는 도구**가 된다.
사용률 축이 %로 정규화되어 있으므로 "80%에 선을 긋는 게 맞는가"를 화면을 보며 논의할 수 있다.

### 1.6 인터랙션 원칙 (챗 ↔ 캔버스 양방향)

- **챗 → 캔버스**: 질문하면 Agent 답변과 함께 관련 위젯이 갱신되고 스크롤/하이라이트로 포커스된다.
- **캔버스 → 챗**: 산점도 점이나 테이블 행(설비)을 클릭하면 해당 설비가 선택되어 하단 상세 존이 갱신되고,
  챗 입력창에 "SCR-014에 대해 자세히 알려줘" 같은 추천 질문이 제안된다.
- **계통도 구간 → 챗**: 계통도의 하이라이트된 구간(pipe)을 클릭하면 그 구간 관련 추천 질문이 제안된다.
- Agent 답변 카드에는 관련 위젯으로 이동하는 링크("산점도에서 보기", "계통도에서 보기")를 포함한다.

### 1.7 표현 원칙 (기획서 10.3 MVP 답변 원칙 + 판단기준 문서 §6.2)

Agent의 모든 텍스트는 단정 대신 후보 표현을 사용한다.

- ❌ "위험합니다"
- ✅ "점검 후보입니다", "동일 PRC그룹 내 상대적으로 높습니다",
  "Pressure 상승이 동반되어 우선 확인이 필요합니다", "현재 기준값이 없어 절대 판단은 어렵습니다"
- 임계값은 교체주기 기반 **추정치**이므로 절대 기준처럼 단정하지 않는다.
  → "추정 임계값 대비 85% 수준입니다" (O) / "임계값을 초과했으므로 위험합니다" (X)
- 상태 판단 답변은 판단기준 문서 §6.2의 근거 제시 형식을 따른다:
  조회 조건 → 상태+판단 케이스 → 근거 수치 → 임시 기준 명시 → 점검 대상 구간 →
  추가 확인 순서 → Pair/BYP 주의사항.

상태 색상도 경보(빨강 일변도)가 아니라 단계형으로: 정상 → 관찰 필요 → 점검 후보 → 원인 확인 필요.

---

## 2. 클로드 디자인 입력 프롬프트 (복사해서 붙여넣기)

아래 블록 전체를 claude.ai에 그대로 입력하면 첫 HTML 초안을 받을 수 있다.
(프롬프트는 자기완결적이다 — 판단기준·계통도 문서 내용 중 필요한 부분이 모두 안에 들어 있다.)

````text
반도체 공장의 PCS(오염방지 배출 계통) 관리 엔지니어를 위한 "Agent 콘솔" 화면을
단일 HTML 파일로 만들어줘. 실제 백엔드 없이 mock 데이터로 동작하는 인터랙티브 프로토타입이야.

## 도메인 배경
- 공정 가스는 메인설비 1대를 루트로 하는 "배기 경로 그래프"를 따라 흐른다:
  정상 흐름:   CHAMBER ─FORELINE→ PUMP ─PS→ NOR밸브 → SCRUBBER ─SD1→ MERGE ─SD2→ DUCT
  바이패스 흐름: NOR밸브 ─SS→ (Pair 상대 설비의) BYP밸브 ─ALLBYPASS→ 상대 MERGE → 상대 DUCT
  가스가 식으면서 배관에 파우더가 누적되면 압력(Pressure)이 상승하고, 심하면 제품 오염
  리스크로 이어진다. 파우더는 구간별로 누적된다: FORELINE(챔버 직후), PS(펌프 후단),
  NOR밸브, SD1(스크러버 후단), MERGE(합류부), 그리고 Pressure Sensor Port(막히면 지시값 왜곡).
- Pair 설비: 일부 설비는 2대가 서로의 스크러버를 바이패스 백업으로 공유한다.
  한쪽 스크러버를 점검(정지)하면 그 설비의 배기가 상대 스크러버로 흘러 부하가 전가되므로,
  Pair 양쪽을 동시에 점검할 수 없고, 상대가 BYP 가동 중이면 관찰이 필요하다.
- PCS기술팀은 설비를 그룹군 계층으로 바라본다:
  사업부(메모리/파운드리) → 사이트(평택/화성/기흥) → 라인(평택=P1/P2, 화성·기흥=15L/16L/17L)
  → 공정(DIFF/CVD/METAL/ETCH) → PRC그룹(ALD_OX, TEOS 등).
  라인이 달라도 PRC그룹이 같으면 비교할 수는 있지만, 라인마다 담당 엔지니어가 다르므로
  기본 뷰는 항상 "내 라인" 단위다.
- 아직 정식 관리 기준값은 없지만, 각 PCS 설비는 사용량에 따른 교체주기 정보가 있어서
  이를 근거로 대략적인 가스 사용량 임계값을 추정한다. 그래서 사용량은 원시 누적량이 아니라
  "최근 교체/클리닝 작업 이후 누적 사용량 ÷ 추정 임계값 = 사용률(%)"로 본다.
  누적 사용량은 교체/클리닝 시점에 리셋된다.
- 총 사용량만으로는 부족하다. 순간 사용량과 공정 스텝이 파우더 생성에 중요하므로,
  해당 PRC그룹에서 사용한 레시피별 "주요 최대(peak) 사용량"과 "1주기당 사용량" 정보를 함께 본다.
- 설비 자체 이상 판단을 위해 현재 상태 파라미터도 본다:
  배기 압력이 상압(대기압)보다 높은지, 배관 구간별 보조 히팅센서(Heating Jacket) 온도가
  정상범위인지. 히팅센서 온도가 낮으면 그 구간에서 가스가 식어 파우더가 잘 생긴다.
- Agent는 "위험합니다"라고 단정하지 않고 "점검 후보입니다", "동일 PRC그룹 내 상대적으로
  높습니다", "추정 임계값 대비 85% 수준입니다" 같은 상대 비교·추정 표현만 쓴다.
  임계값이 추정치라는 것("추정 임계값" 라벨)과 분류 기준이 임시 기준(가정값)이라는 것을
  항상 드러낸다.

## 판단 규칙 (중요: 상태를 하드코딩하지 말고 이 규칙으로 계산)
mock 데이터에는 원시 값만 넣고, 각 설비의 상태는 아래 규칙 함수로 계산해줘.
모든 경계 수치는 "임시 기준(가정값)"이며 UI에도 그렇게 표기한다.

파생 신호 정의:
- 압력높음 = 현재 Pressure > 상압 기준값
- 압력상승 = 최근 7일 추세가 상승 (그리고 최근 24h 평균 > 7일 평균)
- 재상승 = 최근 클리닝/교체 작업 후 7일 이내인데 압력이 작업 전 수준 근처로 돌아옴
- 히팅이탈 = 히팅센서 중 정상범위(특히 하한) 이탈이 1개 이상
- 사용률 경계: 관찰 70% / 점검 85% (산점도 기준선과 연동, 기준선 옮기면 재계산)

판단 케이스 (해당하는 것 모두 수집, 케이스 ID를 결과에 유지):
- J-01: 사용률 ≥70% AND 압력 정상 → 관찰 필요 (교체주기 임박, 예방관리 후보)
- J-02: 사용률 ≥85% AND (압력높음 OR 압력상승) AND 히팅 정상
        → 점검 후보 (파우더 누적 가능성 / 점검 구간: SD1, MERGE, NOR밸브)
- J-03: 사용률 <70% AND 압력높음 AND 히팅이탈
        → 원인 확인 필요 (히팅 이탈 구간의 국부 파우더 또는 Heating Jacket 이상 /
          점검 구간: 이탈 센서가 매핑된 구간)
- J-04: 사용률 <70% AND 압력높음 AND 히팅 정상 AND 최근 작업 후 7일 이내
        → 원인 확인 필요 (Sensor Port 막힘, 작업 범위 누락, 센서 이상 / 점검: Sensor Port부터)
- J-05: 재상승 → 원인 확인 필요 (작업 범위 누락 / 센서·보조장치 문제)
- J-07: Pair 상대 설비가 점검 중(BYP 가동으로 부하 전가받는 중) → 최소 "관찰 필요"로 승격
- 케이스 2개 이상 동시 해당 → "우선 점검 후보"
- 아무 케이스에도 해당 없음 → 정상

우선순위 점수 (테이블 정렬용, 내부 계산):
사용률≥85% +2 / 70~85% +1 / 압력높음 +2 / 압력상승 +1 / 재상승 +2 / 히팅이탈 +2 /
peak 상위 레시피 사용 +1 / Pair 부하 전가 중 +1. 동점이면 압력 높은 쪽 우선.
단, 화면과 답변에는 점수 숫자 대신 "매칭된 케이스 ID + 근거 신호 목록"을 보여줘.
Pair 양쪽이 모두 점검 후보 이상이면 "Pair 관계 — 동시 점검 불가, 순차 점검 필요" 배지 표시.

## 레이아웃 (분할형 하이브리드)
- 좌측 약 30%: 챗 패널. 대화 이력 + 추천 질문 칩 버튼 5개 + 입력창.
- 우측 약 70%: 시각화 캔버스.
  - 최상단에 그룹군 컨텍스트 바 (cascading 셀렉터):
    사업부(메모리/파운드리) → 사이트(평택/화성/기흥) → 라인 → 공정(DIFF/CVD/METAL/ETCH)
    → PRC그룹(ALD_OX/TEOS 등) + 기간(최근 7일/30일) + "데이터 기준시점: 2026-07-04 14:00" 표시.
    상위를 바꾸면 하위 옵션이 종속적으로 바뀌게 (평택→P1/P2, 화성·기흥→15L/16L/17L).
    우측 끝에 "타 라인 동일 PRC그룹 비교" 토글(기본 off, on 하면 산점도에 타 라인 설비가
    반투명 점으로 추가 표시).
  - 캔버스는 2단 구성:

  [상단: 그룹 개요 존] — 선택된 그룹군 전체
    1. 상태 요약 KPI 타일 5개: 정상 / 관찰 필요 / 점검 후보 / 원인 확인 필요 / 우선 점검 후보
       (규칙 함수 결과 집계, 기준선을 옮기면 개수도 재계산)
    2. 사용률 vs Pressure 산점도: X축=추정 임계값 대비 사용률(%), Y축=현재 Pressure.
       X축 기준선 2개(70% 관찰선, 85% 점검선)를 세로 점선으로, Y축에는 상압 기준
       가로 점선. 각 점은 Scrubber이고 상태 색상으로 표시. Pair 부하 전가 중인 설비는
       점에 작은 링 표시.
       영역 라벨: 우상단 "파우더 누적 후보(J-02)", 좌상단 "센서포트/보조장치 확인(J-03·04)",
       우하단 "교체주기 임박(J-01)", 좌하단 "정상".
    3. 점검 후보 테이블: 컬럼 = 순위, 설비, 상태, 판단 케이스(J-xx 뱃지), 사용률(%),
       Pressure(상압 대비/추세), 히팅센서, 최근 작업(경과일), 점검 대상 구간, 추가 확인 항목.
       Pair 제약이 있으면 행에 "Pair: SCR-0XX와 순차 점검" 표시.

  [하단: 선택 설비 상세 존] — 산점도 점이나 테이블 행 클릭 시 갱신
    4. Pressure 시계열 차트: 선택 설비의 최근 30일 압력 추이.
       클리닝/교체 작업 시점을 세로 점선 마커 + 라벨로 오버레이해서
       "작업 후 압력이 다시 올라오는 패턴"이 보이게. 상압 기준선도 가로 점선으로 표시.
    5. 배기 경로 계통도 (이 프로토타입의 핵심 위젯):
       선택된 Scrubber가 속한 메인설비의 배기 경로 그래프를 SVG 노드-엣지 다이어그램으로.
       - 노드 8종을 세로 5단으로 배치: [1단] MAIN·CHAMBER(들) / [2단] PUMP /
         [3단] NOR밸브·SCRUBBER·BYP밸브 / [4단] MERGE / [5단] DUCT
       - 배관(엣지) 6종: FORELINE, PS, SD1, SD2는 실선(정상 경로),
         SS, ALLBYPASS는 점선(바이패스 경로). 배관마다 작은 타입 라벨.
       - 정상 배기 중이면 NOR 경로를 강조. 바이패스 가동 중이면 BYP 경로를 강조하고
         "BYP 가동 중 → SCR-0XX로 배기" 배지 표시.
       - Pair 상대 설비는 오른쪽에 축약 노드(미니 카드)로 표시하고 SS/ALLBYPASS가
         그쪽으로 이어지게.
       - 상태가 점검 후보/원인 확인 필요면 판단 규칙이 지정한 "점검 대상 구간"의
         배관/노드를 상태 색상으로 하이라이트하고, 호버 시 툴팁으로 이유 표시
         (예: "SD1: 사용률·압력 동반 상승 시 파우더 누적 후보 구간").
       - 구간 클릭 시 챗 입력창 위에 "SD1 구간 최근 클리닝 이력 보여줘" 같은 추천 질문 제안.
    6. 교체주기 대비 사용량 게이지: 최근 작업 이후 누적 사용량 / 추정 임계값을
       반원형 게이지 + %로 표시. "추정 임계값 (교체주기 기반)" 라벨과
       "최근 리셋: 배관 클리닝 2026-06-13" 같은 리셋 기준 표시.
       바이패스로 부하를 전가받는 중이면 "타 설비 배기 유입 중 — 누적 가속" 주석.
    7. 레시피별 사용량 테이블: 선택 설비가 속한 PRC그룹에서 사용한 레시피별로
       컬럼 = 레시피명, 주요 최대(peak) 사용량, 1주기당 사용량, 주요 스텝, 비고.
       peak 사용량이 큰 레시피는 시각적으로 강조(막대 인라인 표시 등).
    8. 설비 상태 파라미터 패널:
       - 배기 압력: 현재값 vs 상압 기준값을 나란히 표시, 상압 초과 시 강조.
       - 보조 히팅센서(Heating Jacket) 3~4개: 각 센서가 어느 구간(FORELINE/PS/SD1/MERGE)에
         있는지 구간명과 함께 현재 온도·정상범위 표시, 범위 이탈 센서는 강조.
         이탈 센서의 구간은 계통도 하이라이트와 색이 일치하게.
- 산점도의 점이나 테이블 행을 클릭하면 그 설비가 "선택"되어 하단 상세 존 위젯 4~8이
  모두 해당 설비 기준으로 갱신되고, 챗 입력창 위에 "SCR-014에 대해 자세히 알려줘" 같은
  추천 질문이 뜨게 해줘.

## Mock 데이터
- 기본 컨텍스트: 메모리 사업부 / 평택 / P1 라인 / DIFF 공정 / ALD_OX PRC그룹,
  Scrubber 16개 (SCR-001 ~ SCR-021 중 16개).
- 각 Scrubber는 배기 경로 그래프를 가진다: 메인설비(EQP), CHAMBER 2~4개, PUMP,
  NOR밸브, BYP밸브(있는 경우), MERGE, DUCT + 배관 6종 연결.
  예: EQP-01의 CH-01, CH-02 ─FORELINE→ P-01 ─PS→ NOR밸브 → SCR-001 ─SD1→ MERGE → DUCT.
- 설비별 원시 필드 (상태는 넣지 말 것 — 판단 규칙으로 계산):
  - 추정 임계값(교체주기 기반), 최근 작업 이후 누적 Gas 사용량 → 사용률(%)은 계산
  - 현재 Pressure, 상압 기준값, 30일 Pressure 시계열(일 단위)
  - 히팅센서 3~4개: 구간 매핑(FORELINE/PS/SD1/MERGE) + 현재 온도 + 정상범위
  - 최근 작업 이력(종류/일자), Pair 상대 설비 코드(있는 경우), BYP 가동 여부
- PRC그룹(ALD_OX) 공용 레시피 데이터: 레시피 4~5개, 각각 주요 최대(peak) 사용량,
  1주기당 사용량, 주요 스텝명. peak가 유독 큰 레시피 1개를 포함시켜줘.
- 데이터에 다음 스토리가 반드시 들어가게 만들어줘 (규칙 함수를 통과하면 이 상태가 나오도록
  원시 값을 설계):
  - SCR-001: 사용률 85% + Pressure 상승 추세 + 배관 클리닝 후 21일 경과, 히팅 정상
    → J-02 "점검 후보" (점검 구간: SD1, MERGE, NOR밸브가 계통도에 하이라이트)
  - SCR-014: 사용률 30% + Pressure 상압 초과 + Valve 클리닝 후 3일 경과인데 압력 유지
    + PS 구간 히팅센서 1개가 정상범위 하한 미달
    → J-03(+J-04) "원인 확인 필요" (계통도에 PS 구간 + Sensor Port 하이라이트)
  - SCR-021: 사용률 78% + Pressure 정상 → J-01 "관찰 필요" (예방관리 후보)
  - SCR-007 ↔ SCR-008: Pair 관계. SCR-008은 스크러버 점검 작업 중이라 EQP-08 배기가
    BYP 경로로 SCR-007에 유입 중 → SCR-007은 J-07로 "관찰 필요" 자동 승격
    (계통도에서 SS/ALLBYPASS 점선 경로가 강조되고 "BYP 가동 중" 배지).
  - 나머지는 대부분 정상, 2~3개는 관찰 필요.
- 계층 셀렉터가 실제로 동작하는 걸 보여주기 위해, P2 라인 DIFF/ALD_OX 조합과
  P1 라인 CVD/TEOS 조합에도 소규모(각 4~6개) 설비 데이터를 넣어줘.
  컨텍스트 바를 바꾸면 캔버스 전체가 그 그룹군 데이터로 갱신되게.
- 전체 mock 데이터는 JS 배열/객체로 파일 안에 내장.

## Agent 답변 형식 (모든 상태 판단 답변 공통)
1. 맨 위에 조건 요약 줄:
   [조회 조건: 메모리 / 평택 / P1 / DIFF / ALD_OX · 최근 30일 · 기준시점: 2026-07-04 14:00]
2. 상태 + 판단 케이스: "점검 후보입니다 (J-02: 사용률·압력 동반 상승)"
3. 근거 수치: 사용률 n% (추정 임계값 대비, 리셋: mm-dd 작업), 압력 상태(상압 대비/추세),
   히팅센서 상태
4. "현재 분류는 임시 기준(사용률 85% 등 가정값) 적용 결과입니다" 명시
5. 점검 대상 구간: "스크러버 후단 SD1, MERGE 합류부, NOR밸브"
6. 추가 확인 순서: 센서 지시값 신뢰성 → 보조장치 → 작업 범위 → 바이패스/Pair → 구간 점검
7. 해당 시 제약: "SCR-008과 Pair 관계로 동시 점검 불가" / "바이패스 기간 사용량 미보정"

## 챗 데모 시나리오 (추천 질문 칩 5개, 클릭하면 실제로 동작)
1. "P1 DIFF ALD_OX에서 교체주기 대비 사용률 80% 넘은 설비는?"
   → Agent가 SCR-001, SCR-021 등을 번호 목록으로 답변(위 답변 형식대로, 케이스 ID 포함).
     캔버스는 산점도에서 85% 점검선 주변 점들 하이라이트 + 점검 후보 테이블 갱신.
2. "최근 클리닝 이후에도 Pressure가 다시 오른 설비 있어?"
   → Agent가 SCR-014를 지목: "작업 후 3일 경과인데 압력이 상압보다 높게 유지되고 있고,
     누적 사용률은 30%로 낮습니다. PS 구간 히팅센서 온도가 정상범위 아래로 확인되어
     해당 구간 국부 파우더 또는 Heating Jacket 이상(J-03), Sensor Port 막힘(J-04)
     가능성을 우선 확인해야 합니다. 확인 순서: ① Sensor Port ② PS 구간 Heating Jacket
     ③ 직전 작업 범위"라고 답변.
     캔버스는 SCR-014 선택 → 시계열 차트(작업 마커) + 계통도(PS 구간 하이라이트) +
     상태 파라미터 패널로 포커스.
3. "사용률은 낮은데 Pressure가 높은 설비만 찾아줘"
   → Agent가 좌상단 영역(J-03·04 후보) 설비 목록을 답변.
     산점도가 좌상단 영역만 하이라이트되고 나머지 점은 흐리게.
4. "SCR-001에서 순간 사용량이 큰 레시피는 뭐야?"
   → Agent가 SCR-001이 속한 ALD_OX 그룹의 레시피 중 peak 사용량 상위 레시피를
     수치와 함께 답변("레시피 X의 주요 최대 사용량이 그룹 내 가장 높습니다.
     1주기당 사용량은 …"). 캔버스는 SCR-001 선택 + 레시피별 사용량 테이블 포커스.
5. "지금 바이패스로 배기 중인 설비 있어?"
   → Agent가 SCR-008 점검 중으로 EQP-08 배기가 SCR-007로 바이패스되고 있음을 답변:
     "SCR-007은 부하 전가 기간 동안 관찰 필요로 분류했습니다(J-07). 이 기간의 누적
     사용량 증가분은 SCR-007에 귀속됩니다. 또한 SCR-007·SCR-008은 Pair 관계이므로
     동시 점검이 불가합니다." 캔버스는 SCR-007 선택 + 계통도에서 BYP 경로(SS/ALLBYPASS
     점선) 강조 + "BYP 가동 중" 배지.
- 자유 입력도 받되, 매칭되는 시나리오가 없으면 "프로토타입에서는 5가지 데모 질문을
  지원합니다"라고 안내.

## 디자인 톤
- 다크 테마 모니터링 콘솔 (엔지니어링 관제 화면 느낌).
- 상태 색상: 정상=녹색 계열, 관찰 필요=노랑, 점검 후보=주황, 원인 확인 필요=붉은 계열,
  우선 점검 후보=붉은 계열+강조 테두리. 단, 경보성 빨강 남발 금지 — 차분한 톤으로.
- 계통도의 구간 하이라이트 색은 해당 설비의 상태 색과 일치시켜서
  "테이블 → 계통도 → 상태 패널"이 같은 색 언어로 읽히게.
- UI 텍스트는 한국어, 설비명/필드명/구간명은 영문 유지(SCR-001, Pressure, ALD_OX, SD1).

## 기술 제약
- 외부 CDN/라이브러리 없이 단일 HTML 파일 (HTML + CSS + Vanilla JS).
- 차트·계통도는 SVG 직접 렌더링.
- 상태 분류·스코어링은 순수 JS 함수로 분리해서(예: classifyEquipment(eq, thresholds))
  기준선 드래그 등으로 thresholds가 바뀌면 전체 화면이 재계산되게.
- 1440px 데스크톱 기준으로 만들되 좁아지면 상하 배치로 접히게.
````

---

## 3. 프롬프트 활용 가이드

### 3.1 진행 방법

1. 위 프롬프트를 그대로 입력해 **첫 초안**을 받는다.
2. 초안에서 먼저 확인할 것 두 가지:
   - **시나리오 2번(SCR-014)**: 판단 규칙이 J-03/J-04를 제대로 분류하고,
     계통도에 PS 구간이 하이라이트되는가 (규칙 함수 + 구간 특정이 v3의 핵심)
   - **시나리오 5번(바이패스)**: 계통도의 BYP 경로 표시와 J-07 승격이 동작하는가
3. 한 번에 전부 고치려 하지 말고, 시나리오 1개 → 위젯 1개 순서로 반복 개선한다.
4. 레이아웃 골격(그룹 개요 존 ↔ 선택 설비 상세 존의 2단 흐름)이 마음에 들면
   그때 세부 위젯을 다듬는다.

### 3.2 반복 개선용 후속 프롬프트 예시

```text
산점도의 사용률 기준선 2개(70% 관찰선, 85% 점검선)를 드래그로 옮길 수 있게 해줘.
기준선을 옮기면 classifyEquipment가 재실행되어 KPI 타일, 점검 후보 테이블,
계통도 하이라이트까지 전부 재계산되게. (관리 기준점 P-01/P-02 논의용)
```

```text
"타 라인 동일 PRC그룹 비교" 토글을 켜면 P2 라인 ALD_OX 설비들이 산점도에
반투명 점으로 표시되게 해줘. 단, 점검 후보 테이블과 KPI는 여전히 P1만 집계.
(담당 라인은 유지하되 타 라인과 분포만 비교하는 용도)
```

```text
계통도에서 구간(pipe)을 클릭하면 그 구간의 상세 팝오버가 뜨게 해줘:
구간 타입(SD1 등), 매핑된 히팅센서 온도, 이 구간이 점검 후보인 이유,
"이 구간 클리닝 이력 보여줘" 추천 질문 버튼.
```

```text
점검 후보 테이블 행을 펼치면 Agent 답변과 같은 형식의 "판단 근거 카드"가 나오게 해줘:
매칭된 판단 케이스(J-xx), 근거 신호 목록, 적용된 임시 기준값, 점검 대상 구간,
추가 확인 순서, Pair/BYP 주의사항.
```

```text
"점검 시뮬레이션" 모드를 추가해줘. 설비를 하나 골라 "점검 시작"을 누르면
그 설비가 BYP 가동 상태가 되어 Pair 상대 설비의 계통도·게이지·상태 분류가
어떻게 바뀌는지(J-07 승격, 부하 전가 주석) 미리 볼 수 있게. (Pair 제약 교육/논의용)
```

```text
레시피별 사용량 테이블을 peak 사용량 기준으로 정렬/필터할 수 있게 하고,
특정 레시피를 클릭하면 그 레시피를 사용하는 설비들이 산점도에서 하이라이트되게 해줘.
```

### 3.3 이 초안의 역할

- 완성된 HTML은 그 자체가 **Phase 1 MVP 챗 UI의 화면 요구사항 문서** 역할을 한다.
  개발 착수 시 "이 화면처럼"이라고 전달하면 된다.
- v3의 규칙 함수(classifyEquipment)는 판단 기준 정의서의 **J-매트릭스 구현체**다.
  프로토타입에서 규칙과 수치를 만져보는 것 자체가 Phase 3 Rule Engine의 사전 검증이 된다.
- **기준선 드래그 + 재계산**(3.2 첫 번째)은 "추정 임계값의 몇 %를 관리 기준점으로
  삼을 것인가"(P-01/P-02)를, **점검 시뮬레이션**(3.2 다섯 번째)은 Pair/바이패스 제약(B3/B4)을
  엔지니어와 논의하는 워크숍 도구다.
- 현업 엔지니어에게 초안을 보여줄 때 확인할 것 — 판단 기준 정의서 §7 워크숍 질문과 연동:
  - 그룹군 셀렉터의 계층·명칭이 실제 조직의 데이터 뷰와 맞는가
  - 교체주기 기반 사용률(%) 표현과 임시 경계값(70/85%)이 현장 감각과 맞는가 (Q1)
  - 계통도의 구간 하이라이트가 실제 점검 안내로 쓸 만한 수준인가, 압력 측정 지점은 어디인가 (Q2)
  - 히팅센서-구간 매핑이 실제 데이터로 가능한가 (Q3)
  - 바이패스 전환 이력이 데이터로 남는가 — 시나리오 5번이 실제로 구현 가능한가 (Q5)
  - 챗으로 물어볼 것과 화면에서 바로 볼 것의 경계가 어디인가
